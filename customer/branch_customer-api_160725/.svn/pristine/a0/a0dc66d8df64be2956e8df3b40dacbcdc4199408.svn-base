package test;

import junit.framework.TestCase;

public class DepartmentServiceTest extends TestCase {

	/*
	 * private final DepartmentService departmentService = new
	 * DepartmentServiceImpl();
	 * 
	 * @Test public void test() throws Exception { // createDepartment(); //
	 * findById(); findByParams(); // getCustomByDeptId(); // getRoleByDeptId();
	 * }
	 * 
	 * public void createDepartment() throws Exception {
	 * 
	 * // 部门 ->用户列表 -> 角色列表 -> 菜单列表
	 * 
	 * Department department = new Department(); department.setName("testDept");
	 * department.setType("1"); department.setAreaId("101");
	 * department.setGrade("1");
	 * 
	 * // 角色 Role role = new Role(); role.setName("testRole");
	 * role.setCreateBy("test"); role.setUpdateBy("testUpdate");
	 * 
	 * // 菜单 Menu menu = new Menu(); menu.setName("testMenu");
	 * menu.setCreateBy("test"); menu.setUpdateBy("testUpdate");
	 * 
	 * // 菜单列表 List<Menu> menuList = new ArrayList<Menu>(); menuList.add(menu);
	 * // 新增部门时,角色和菜单的权限已设置好,不用再处理逻辑 // role.setMlist(menuList);
	 * 
	 * // 角色列表 List<Role> roleList = new ArrayList<Role>(); roleList.add(role);
	 * // department.setList(roleList);
	 * 
	 * // 用户 Customer custom = new Customer(); custom.setName("testCustom");
	 * custom.setLoginName("testLoginName"); custom.setLoginPasswd("testpwd");
	 * custom.setUserType("1"); custom.setCreateTime(new Date());
	 * 
	 * custom.setRoleList(roleList); // 设置菜单和用户的关系 custom.setMenuList(menuList);
	 * 
	 * List<Customer> customList = new ArrayList<Customer>();
	 * customList.add(custom); department.setClist(customList);
	 * 
	 * departmentService.createDepartment(department); }
	 * 
	 * public void updateDepartment() throws Exception { Department department =
	 * departmentService.getDepartmentById(2L); assertNotNull(department);
	 * 
	 * department.setName("testUpdate");
	 * 
	 * departmentService.saveDepartment(department); }
	 * 
	 * public void getCustomByDeptId() throws Exception { Department dept = new
	 * Department(); dept.setId(3L);
	 * 
	 * List<Department> deptList = new ArrayList<Department>();
	 * deptList.add(dept);
	 * 
	 * List<Department> result = departmentService
	 * .getDepartmentCustomerList(deptList); assertNotNull(result); }
	 * 
	 * public void getRoleByDeptId() throws Exception { Department dept = new
	 * Department(); dept.setId(3L);
	 * 
	 * List<Department> deptList = new ArrayList<Department>();
	 * deptList.add(dept);
	 * 
	 * List<Department> result = departmentService
	 * .getDepartmentRoleList(deptList); assertNotNull(dept); }
	 * 
	 * public void deleteById() throws Exception {
	 * departmentService.deleteDepartment(2L); }
	 * 
	 * public void findById() throws Exception { Department dept =
	 * departmentService.getDepartmentById(3L); assertNotNull(dept); }
	 * 
	 * public void findByParams() throws Exception { Department department = new
	 * Department(); department.setId(15L); // department.setName("testDept");
	 * 
	 * PageList<Department> list = departmentService.findDepartmentByParams(1,
	 * 10, department); assertNotNull(list);
	 * assertNotNull(list.getResultList()); }
	 * 
	 * public static void main(String[] args) { Role r = new Role();
	 * r.setId(2L);
	 * 
	 * SysRole role = new SysRole(); BeanUtils.copyProperties(r, role);
	 * 
	 * System.out.println(role.getId()); }
	 */
}
